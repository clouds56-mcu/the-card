[project]
name = "the-card-hw"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "easyeda2kicad>=1.0.1",
  "pyyaml>=6.0",
  "skidl>=2.3.0",
]

[dependency-groups]
dev = [
  "pypdf>=6.14.2",
]
